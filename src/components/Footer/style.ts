import styled from 'styled-components'

export const Container = styled.footer`
  width: 100%;
  max-width: 62rem;
  margin: 0 auto;

  min-height: 120px;

  display: flex;
  justify-content: center;
  align-items: center;

  p {
    font-size: 1rem;
    font-weight: 300;
    color: var(--grayLight);
  }
`
